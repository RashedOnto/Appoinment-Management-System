
@{
    var user = @User.Identity.Name;
}
<link href="~/sourcecode/bootstrap.min.css" rel="stylesheet" />
@if (string.IsNullOrWhiteSpace(user))
{
  @*  <ul class="navbar-nav ml-auto ml-md-0">
        <li class="nav-item">
            <a class="nav-link" href="/Identity/Account/Login">Login</a>
        </li>

        <li class="nav-item">
            <a class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Register">Register</a>
        </li>
    </ul>*@
}
else
{
    <ul class="navbar-nav ml-auto ml-md-0">
        <li class="nav-item dropdown">
            <a class="nav-link text-white" id="userDropdown" href="#" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                <i class="fas fa-user-circle" style="height:35px;width:35px;"></i>
            </a>
            <div class="dropdown-menu dropdown-menu-right" aria-labelledby="userDropdown">
                <p class="dropdown-item-text"></p>
                <form class="form-inline" asp-area="Identity" asp-page="/Account/Manage/ChangePassword" asp-route-returnUrl="@Url.Page("/", new { area = "" })" method="post">
                    <button type="submit" class="dropdown-item btn btn-link text-dark">Change Password</button>
                </form>
                <form class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })" method="post">
                    <button type="submit" class="dropdown-item btn btn-link text-dark">Logout</button>
                </form>
            </div>
        </li>
    </ul>
}

<script src="~/sourcecode/jquery-3.3.1.slim.min.js"></script>
<script src="~/sourcecode/bootstrap.min.js"></script>
