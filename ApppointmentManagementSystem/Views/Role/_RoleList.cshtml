
<table class="table table-striped table-bordered custrom-table mb-2 mt-4" id="table">
    <thead>
        <tr>
            <th>Name</th>
            <th>Order No</th>
            <th>Action</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Name</td>
                <td>@item.OrderNo</td>
                <td>
                    <button type="button" class="btn btn-sm btn-primary" onclick="EditRole('@item.Id', '@item.Name',@item.OrderNo)">Edit</button>
                    <button class="btn btn-sm btn-danger" onclick="DeActiveRole('@item.Id')">Delete</button>
                </td>
            </tr>
        }

    </tbody>
</table>

<script>
    $(document).ready(function () {
        $('#table').DataTable();
    });
</script>